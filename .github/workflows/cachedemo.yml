name: Cache Demo
on:
  workflow_dispatch:

jobs:
  cache-demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/cache@v4
        with:
          path: demo/folder
          key: cachedemofile

      - run: |
          mkdir demo-folder
          echo "GitHub Actions Cache"> demo-folder/cache.txt

