name: Passing Inputs Demo

on:
  workflow_call:

    inputs:
      username:
        required: true
        type: string

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - run: |
            echo "Hello ${{inputs.username}}"
            
  