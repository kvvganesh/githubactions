name: Matrix Demo

on:
  workflow_dispatch:

jobs:
    matrixdemo:
      strategy:
        matrix:
          os:
            - ubuntu-latest

            - windows-latest

            - macos-latest

      runs-on: ${{matrix.os}}
      steps:
        - name: Show Operating System
          run: echo "Running On= ${{matrix.os}}"